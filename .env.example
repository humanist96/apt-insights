# 아파트 실거래가 분석 플랫폼 - 환경변수 템플릿
# 실제 사용 시 이 파일을 .env로 복사하고 실제 값을 입력하세요
# WARNING: .env 파일은 절대 Git에 커밋하지 마세요!

# 국토교통부 공공데이터 API 서비스 키
# 발급: https://www.data.go.kr/
SERVICE_KEY=your_molit_api_key_here

# 데이터베이스 설정 (Phase 1에서 사용)
DATABASE_URL=postgresql://user:password@localhost:5432/apt_insights

# Redis 캐시 (Phase 1에서 사용)
REDIS_URL=redis://localhost:6379/0

# JWT 인증 시크릿 키 (Phase 2에서 사용)
# 생성 방법: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your_secret_key_here

# Google Gemini API (AI 인사이트용)
GEMINI_API_KEY=your_gemini_api_key_here

# PortOne 결제 (Phase 2에서 사용)
PORTONE_API_KEY=your_portone_api_key_here
PORTONE_API_SECRET=your_portone_api_secret_here

# 운영 환경 설정
ENVIRONMENT=development  # development, staging, production

# 로깅 레벨
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# 외부 서비스 URL (Phase 2에서 사용)
FRONTEND_URL=http://localhost:8501
BACKEND_URL=http://localhost:8000
